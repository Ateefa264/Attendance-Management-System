# Attendance-Management-System
A C++-based Attendance and Leave Management System that records employee attendance, processes multiple leave types, manages approvals, tracks leave balances, and generates reports. All data is stored using file handling, with no database required.
